{ Lsrm.inc }




  {$DEFINE COMMAND_LINE}

  (*
  ѕри реальной работе логин, пароль и адрес сервера ввод€тс€ пользователем в другом приложении. —юда они передаютс€ через
  параметры командной строки.
  “.е. дл€ запуска нужно в строке написать, например:
  UserBase.exe "LocalHost:Lsrm" "sysdba" "masterkey"
  ѕри отладке это неудобно. ѕомогает директива COMMAND_LINE.
  ≈сли она не объ€влена (строка закомментирована), то действуют описанные правила.
  ≈сли директива есть, то вместо параметров командной строки подставл€ютс€ константы описанные в модуле ClientDM,
  строка 40 и далее и модуль Prima, строка 195 и далее.
  ѕеред тем как выложить новый EXE на боевую комментировать директиву об€зательно! »наче просто работать ничего не будет...

  *)

